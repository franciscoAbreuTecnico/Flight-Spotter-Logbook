package com.flightspotterlogbook.model;

/**
 * Represents the visibility of a sighting. PUBLIC sightings are visible to all users,
 * PRIVATE sightings are only visible to the owner.
 */
public enum Visibility {
    PUBLIC,
    PRIVATE
}